import { ReadableTypePipe } from './readable-type.pipe';

describe('ReadableTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ReadableTypePipe();
    expect(pipe).toBeTruthy();
  });
});
