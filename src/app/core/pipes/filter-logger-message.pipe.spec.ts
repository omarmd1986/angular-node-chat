import { FilterLoggerMessagePipe } from './filter-logger-message.pipe';

describe('FilterLoggerMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLoggerMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
